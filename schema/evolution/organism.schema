package evolution.organism;
import "evolution/environment.schema";

type Gene {
	string gene_char = 1;
}

type Reproduce {
	
}

type MaterialLimit {
	evolution.environment.Material material = 1;
	uint32 limit = 2;
}

type Effect {
	
}

component Consumer {
	id = 1000;
	evolution.environment.Material consumes = 1;
	evolution.environment.Material produces = 2;
	double radius = 3;
}

component Reproducer {
	id = 1001;
	list<Gene> genome1 = 1;
	list<Gene> genome2 = 2;
	event Reproduce reproduce;
}

component Mover {
	id = 1002;
	uint32 speed = 1;
}

component Affectable {
	list<MaterialLimit> limit_list = 1;
	event Effect taken_damage;
}